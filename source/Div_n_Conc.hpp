#ifndef DNC_HPP
#define DNC_HPP

#include "point.hpp"
#include <vector>
#include <cmath>

//std::pair<std::vector<point>, std::vector<point>> gen_axis(std::vector<point> const& vec_o) {}

std::pair<point, point> find_closest_points(std::vector<point> const& x, std::vector<point> const& y) {}

std::pair<point, point> combine(std::vector<point> const& y, int l_x, std::pair<point, point> const& p1, std::pair<point, point> const& p3) {}

#endif